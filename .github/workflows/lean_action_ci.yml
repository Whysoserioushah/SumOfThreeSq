name: Lean Action CI

on:
  push:
  pull_request:
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read # Read access to repository contents
  pages: write # Write access to GitHub Pages
  id-token: write # Write access to ID tokens

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: jcreedcmu/docgen-action@2be5bc319536b0c76a341ce0e9684aa2e4832358
        with:
          lake-package-directory: "lean_content"
      - uses: leanprover-community/docgen-action@1417dc7f90338c875da5e5870c03a287d8348896
        with: 
          blueprint: true
          api_docs: true
          lake_package_directory: "lean_content"
